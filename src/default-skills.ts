export interface DefaultSkill {
  name: string;
  body: string;
  files?: Record<string, string>;
}

const FEISHU_SKILL_MAKER = `---
name: feishu-skill-maker
version: 1.0.0
description: "创建和维护 Feishu Skill 的规范与工作流。当用户要求创建、新增、编写、修改或发布一个 Feishu Skill（把飞书操作封装成可复用的 SKILL.md）时使用。"
---

# Feishu Skill Maker

Feishu Skill 是只对 Feishu Agent 可见的按需能力包，形式为一份 \`SKILL.md\`。本文件同时是规范与执行步骤：新建 Skill 时必须逐条遵循。

## 存放位置与优先级

1. 项目私有：\`<project>/.feishu-agent/skills/<name>/SKILL.md\` —— 只在该 Feishu Project 生效
2. 全局私有：\`~/.feishu-agent/skills/<name>/SKILL.md\` —— 跨项目复用（**用户未指定位置时默认放这里**）
3. 安装的 Feishu Package、官方 lark-cli 缓存（只读，不要手工改）

同名 Skill 最终生效顺序：项目私有 > 全局私有 > 安装包 > 官方缓存；被遮蔽的来源会在启动时告警。不要创建与现有官方 Skill（如 lark-im、lark-mail）同名的技能，避免无意遮蔽。

## 目录结构

\`\`\`
skills/<name>/
  SKILL.md           # 必填
  references/*.md    # 可选，SKILL.md 用相对路径引用
\`\`\`

## SKILL.md 规范

- frontmatter 必填 \`name\` 和 \`description\`；\`name\` 用 kebab-case，飞书域技能建议 \`feishu-\` 或 \`lark-\` 前缀。
- \`description\` 用中文写清“做什么 + 什么时候用”，包含触发词（如“创建 skill”“发审批”），这是模型自动加载和 \`/skill:<name>\` 检索的依据。
- 正文先写触发条件，再写分步操作；每步给出可直接执行的 \`lark-cli\` 命令。优先级：Shortcut（\`+xxx\`）> 已注册 API > \`lark-cli api\` 裸调。
- 个人资源命令默认带 \`--as user\`；只有用户明确要求或接口强制时用 \`--as bot\`。
- 高危写操作（删除/移除/撤销/撤回）只有在用户本轮消息明确要求该类动作时才能带 \`--yes\`，不得自行推断；无 \`--yes\` 时交给 lark-cli 自己的确认提示。
- 不写入、不复制、不打印任何 Token/API Key；认证复用 lark-cli 现有登录态或环境变量名，不落地密钥。
- 首次创建前用 \`lark-cli schema <service.resource.method>\` 或 \`lark-cli <service> --help\` 核对参数，禁止编造标志。
- 不确定的命令写 \`--help\` 查询而不是猜。

## 创建流程

1. 确认需求属于飞书交付或 lark-cli 工作流；否则转交普通 pi。
2. 确定存放位置（默认全局 \`~/.feishu-agent/skills/\`）。
3. 写 \`SKILL.md\`，遵循上面的 frontmatter 与命令规范。
4. 自检：\`name\` 唯一且不与官方缓存冲突；\`description\` 含触发词；所有命令真实存在（\`--help\`/\`schema\` 验证过）。
5. 告知用户保存路径与生效范围，并提示可用 \`/skill:<name>\` 手动调用。

## 最小示例

\`\`\`markdown
---
name: feishu-calendar-review
description: "查看并总结飞书日历日程。当用户要查今天/本周的日程安排时使用。"
---

# Calendar Review

1. 查日程：\`lark-cli calendar +agenda --as user\`
2. 用中文总结当天安排；无日程则如实说明。
\`\`\`
`;

const FEISHU_LATEX_RENDERING = `---
name: feishu-latex-rendering
version: 1.0.0
license: MIT
description: "飞书云文档（Docx / XML）与 Markdown 场景下的数学公式（LaTeX）渲染规范与排坑指南。解决从 Markdown/LaTeX 到飞书云文档时 \`$ ... $\` 与 \`$$ ... $$\` 无法渲染、字符转义报错、行内与独立块排版、表格/高亮块嵌套等问题。"
metadata:
  requires:
    bins: ["lark-cli"]
    skills: ["lark-doc", "lark-markdown", "lark-shared"]
---

# 飞书数学公式（LaTeX）渲染规范与排坑指南

本文档为在飞书云文档（Docx / XML）中编写、转换与更新**数学公式（LaTeX）**的标准规范与排坑手册。

---

## 1. 核心渲染原理与致命死穴

### ❌ 常见错误与现象
- **现象**：在 Markdown 或 XML 中直接写入 \`$E=mc^2$\` 或 \`$$\\log p(x) = \\dots$$\`，发布到飞书云文档后**原样显示为带有 \`$\` 符号的纯文本**，公式未被渲染。
- **根源**：飞书 Docx 富文本引擎与底层 XML 解析器**不识别 Markdown 风格的 \`$\` / \`$$\` 语法**，飞书原生公式必须使用专用标签 \`<latex>...</latex>\`。

### ✅ 正确语法对照表

| 场景 | Markdown 习惯写法（❌ 在飞书 XML 不渲染） | 飞书 XML 原生写法（✅ 正确渲染） |
| :--- | :--- | :--- |
| **行内公式 (Inline)** | \`$v_\\theta(x_t, t)$\` | \`<latex>v_\\theta(x_t, t)</latex>\` |
| **独立行公式 (Block)** | \`$$\\mathcal{L}(\\theta) = \\mathbb{E}[\\|v - u\\|^2]$$\` | \`<p><latex>\\mathcal{L}(\\theta) = \\mathbb{E}[\\|v - u\\|^2]</latex></p>\` |
| **表格单元格** | \`<td>$x_t = (1-t)x_0 + tx_1$</td>\` | \`<td><p><latex>x_t = (1-t)x_0 + tx_1</latex></p></td>\` |
| **高亮块 (Callout)** | \`<callout><p>$$\\nabla_\\theta L = 0$$</p></callout>\` | \`<callout><p><latex>\\nabla_\\theta L = 0</latex></p></callout>\` |

---

## 2. 飞书 XML 公式排版与嵌套规范

### 2.1 容器包裹铁律
\`<latex>\` 是**行内级元素**，必须包裹在块级容器标签内（如 \`<p>\`、\`<td>\`、\`<th>\`、\`<li>\`、\`<blockquote>\`）：
\`\`\`xml
<!-- ✅ 正确：包裹在 <p> 中 -->
<p>瞬时速度定义为：<latex>v_t = \\frac{dx_t}{dt}</latex>，其中时间 <latex>t \\in [0, 1]</latex>。</p>

<!-- ✅ 正确：独立公式块 -->
<p><latex>\\log p_1(x_1) = \\log p_0(x_0) - \\int_0^1 \\text{Tr}\\left(\\frac{\\partial v_t}{\\partial x}\\right) dt</latex></p>

<!-- ❌ 错误：顶层直接使用 <latex>（会导致 XML 解析失败或渲染丢失） -->
<latex>E = mc^2</latex>
\`\`\`

### 2.2 XML 特殊字符转义红线
由于 \`<latex>\` 存在于 XML 文档中，**LaTeX 公式内部不能出现未转义的 XML 保留字符**：

| LaTeX 字符 | 冲突原因 | 安全替代写法 |
| :--- | :--- | :--- |
| \`<\` (小于号) | 破坏 XML 标签开始符 | 使用 \`\\lt\`、\`\\le\`（小于等于）或 \`&lt;\` |
| \`>\` (大于号) | XML 标签结束符 | 使用 \`\\gt\`、\`\\ge\`（大于等于）或 \`&gt;\` |
| \`&\` (对齐符号) | XML 实体起始符 | 在单行公式避免 \`&\`；若必须使用转义为 \`&amp;\` |

**示例**：
\`\`\`xml
<!-- ❌ 错误：< 会导致 XML 解析报错 -->
<p><latex>t < 1.0</latex></p>

<!-- ✅ 正确：使用 \\lt 或 \\le -->
<p><latex>t \\lt 1.0</latex> 或 <latex>t \\le 1.0</latex></p>
\`\`\`

---

## 3. 支持的 LaTeX 语法特性

飞书内置 KaTeX/MathJax 兼容子集，支持主流数学符号与宏：
- **微积分与算子**：\`\\int_0^1\`, \`\\sum_{i=1}^n\`, \`\\frac{\\partial v}{\\partial x}\`, \`\\nabla_\\theta\`, \`\\text{Tr}(\\cdot)\`, \`\\mathbb{E}\`
- **希腊字母与矩阵**：\`\\alpha\`, \`\\theta\`, \`\\sigma_{min}\`, \`\\mathcal{L}\`, \`\\mathbb{R}^{d}\`, \`\\mathbf{X}\`
- **箭头与关系符**：\`\\to\`, \`\\sim\`, \`\\approx\`, \`\\Longrightarrow\`, \`\\mid\`, \`\\cdot\`, \`\\otimes\`
- **范数与括号**：\`\\| x \\|^2\`, \`\\left( \\dots \\right)\`, \`\\left[ \\dots \\right]\`, \`\\{ \\dots \\}\`

---

## 4. 自动化转换脚本（Markdown → 飞书 XML）

当有包含 \`$...$\` 与 \`$$...$$\` 的 Markdown 或草稿文本时，可使用以下 Python 正则脚本一键替换：

\`\`\`python
import re

def convert_markdown_math_to_feishu_xml(text: str) -> str:
    """
    将 Markdown 风格的 $$ ... $$ 和 $ ... $ 自动转换为飞书 XML 的 <latex> 标签
    并自动处理 XML 实体转义
    """
    # 1. 替换多行独立公式 $$ ... $$ -> <p><latex> ... </latex></p>
    def replace_block_math(match):
        formula = match.group(1).strip()
        # 转义 < 和 &
        formula = formula.replace('&', '&amp;').replace('<', r'\\lt ')
        return f"<p><latex>{formula}</latex></p>"

    text = re.sub(r'\\$\\$(.*?)\\$\\$', replace_block_math, text, flags=re.DOTALL)

    # 2. 替换单行行内公式 $ ... $ -> <latex> ... </latex>
    def replace_inline_math(match):
        formula = match.group(1).strip()
        formula = formula.replace('&', '&amp;').replace('<', r'\\lt ')
        return f"<latex>{formula}</latex>"

    text = re.sub(r'(?<!\\$)\\$(?!\\$)(.*?)(?<!\\$)\\$(?!\\$)', replace_inline_math, text)
    return text
\`\`\`

---

## 5. 快速检查 Checklist

在执行 \`lark-cli docs +create\` 或 \`lark-cli docs +update\` 之前，必须执行自检：
1. [ ] 全文搜索 \`$\`：确认**无遗留的 \`$ ... $\` 或 \`$$ ... $$\`**；
2. [ ] 检查 \`<latex>\` 内部：确认没有裸写 \`<\` 或未转义 \`&\`；
3. [ ] 检查父容器：确认每个 \`<latex>\` 都处于 \`<p>\`、\`<td>\`、\`<th>\` 或 \`<li>\` 内部；
4. [ ] 预览验证：在飞书云文档中打开网页版，确认公式无文本乱码并正常居中/行内渲染。
`;

const PROCESS_OPTIMIZATION_BIWEEKLY = `---
name: process-optimization-biweekly
version: 1.0.0
license: MIT
description: "工艺优化（如目标材料专题、配方与烧结优化等）周报与双周例会进展的完整调研、信息获取与写作规范。包含 IM 单聊/群聊/排期表/技术文档的信息获取全景地图，以及按项目生命周期 5 阶段演进的硬核工程师写作模版与飞书操作流。"
metadata:
  requires:
    bins: ["lark-cli"]
    skills: ["lark-doc", "lark-sheets", "lark-im", "lark-contact", "lark-shared"]
---

> 这是公开的可复用模板。使用前请把尖括号占位符替换为当前项目的资源标识；不要把真实 Open ID、Chat ID、文档或表格 token 写入 Skill。
> 优先通过名称搜索资源，或使用用户在当前任务中明确提供的标识。

# 工艺优化周报与双周例会：信息获取与写作规范

本文档为工艺优化（以目标材料专题为标杆）算法/研发工程师在进行**周报、双周例会进展梳理、下一步工作规划及飞书文档更新**时的标准化操作手册。

---

## 一、 每周从哪里获取内容（信息源全景地图）

工艺优化每周的工作内容随项目推进不断变化。撰写前必须从以下 **4 类信息源** 提取客观事实：

\`\`\`
┌─────────────────────────────────────────────────────────────────────────────┐
│                            工艺优化周报信息源地图                            │
├───────────────────────┬─────────────────────────┬───────────────────────────┤
│    1. IM 即时通讯     │     2. 项目进度排期     │      3. 专项技术资产      │
│  • P2P单聊(PM/专家)   │  • 排期底表(Sheet)      │  • 专题方案与备忘(Docx)   │
│  • 项目/指标/算法群聊 │  • 责任人任务行与 Note  │  • 数据字典与门禁标准     │
└───────────────────────┴─────────────────────────┴───────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    4. 目标周例会 / 双周报文档 (Docx)                        │
│             • 提取第 3 节「当前进展」与第 4 节「下一步计划」                │
└─────────────────────────────────────────────────────────────────────────────┘
\`\`\`

### 1. IM 单聊与群聊监控清单

#### A. 核心协同人单聊（P2P）
| 对接人 | 角色 / 部门 | 检索目的与关注要点 | 常用 OpenID / Chat ID |
|---|---|---|---|
| **项目经理** | PM / 项目经理 | 获取本周例会议程、客户催办、截稿提醒、硬件/商务进展、待办确认 | \`<OPEN_ID>\`<br>\`<CHAT_ID>\` |
| **算法专家** | 架构师 / 算法专家 | 获取算法选型共识、指标量化定义、评测基准探讨、技术方案评审意见 | 通讯录检索 \`算法专家\` |
| **表征负责人** | 表征算法负责人 | 获取物性表征与工艺算法的输入对齐（如 SEM 图像特征、粒度 D50 作为工艺变量） | 通讯录检索 \`表征负责人\` |
| **平台负责人** | 平台交付负责人 | 获取算法工具封装、平台 API 接口联调、私有化部署环境与数据安全要求 | 通讯录检索 \`平台负责人\` |
| **客户技术专家** | 客户方技术专家 | 获取客户历史批次台账提供进度、异常字段说明、实验工况与排产反馈 | 单聊或项目群 |

#### B. 核心协同群聊（Group）
| 群聊名称 | Chat ID | 关注内容与检索关键词 |
|---|---|---|
| **项目总群** | \`<CHAT_ID>\` | 硬件配置（GPU/算力）、商务预付款、高层例会通知与整体交付节奏 |
| **项目材料群** | \`<CHAT_ID>\` | 项目总体排期变更、主方案合稿、客户周例会日程通知 |
| **交付指标群** | \`<CHAT_ID>\` | 验收指标确认意见、评测集定义、算法精度与实验指标量化标准 |
| **算法讨论群** | \`<CHAT_ID>\` | 贝叶斯优化（BO）、高维采样、多目标寻优、基线模型对比讨论 |

#### C. IM 常用提取命令
\`\`\`bash
# 1. 检查 PM（项目经理）最近 10 条聊天记录
python3 -c "
import subprocess, json
res = subprocess.run(['lark-cli', 'im', '+chat-messages-list', '--chat-id', '<CHAT_ID>', '--as', 'user'], capture_output=True, text=True)
for msg in json.loads(res.stdout).get('data', {}).get('messages', [])[-10:]:
    print(f\\"[{msg.get('create_time')}] {msg.get('sender',{}).get('name')}: {msg.get('msg_type')} -> {msg.get('content')[:120]}\\")
"

# 2. 检查群聊内最新文件或指标讨论
lark-cli im +chat-messages-list --chat-id "<CHAT_ID>" --as user
\`\`\`

---

### 2. 项目进度排期底表（Sheet）

- **表格定位**：飞书电子表格《项目进度排期》（Token: \`<SPREADSHEET_TOKEN>\`）。
- **重点提取字段**：
  \`\`\`bash
  # 读取当前周对应的任务状态与 Note
  python3 -c "
  import subprocess, json
  res = subprocess.run(['lark-cli', 'sheets', '+read', '--spreadsheet-token', '<SPREADSHEET_TOKEN>', '--range', '<SHEET_RANGE>', '--as', 'user'], capture_output=True, text=True)
  for r in json.loads(res.stdout)['data']['valueRange']['values'][1:]:
      if len(r) > 2 and '项目负责人' in str(r[2]):
          print(f\\"Task {r[0]}: {r[1]} | 状态: {r[9] if len(r)>9 else ''} | 备注: {r[11] if len(r)>11 else ''}\\")
  "
  \`\`\`
- **关键任务映射**：
  - \`Task 18\`：LFP 专题需求沟通（目标、基线、核心变量、实验验证资源）
  - \`Task 19\`：LFP 历史数据字段整理（配方、细磨、烧结、前驱体物性、检测指标）
  - \`Task 20\`：LFP 数据质量评估（缺失、异常、批次链路追溯）
  - \`Task 21 & 22\`：LFP 需求整理与评审会（M1 基线冻结）
  - \`Task 42~47\`：LFP 模型训练、离线评测、候选方案推荐、实验验证、模型迭代

---

### 3. 专项技术与方案资产（Docx / Base）

- **技术基准文档**：如 [《目标材料专题模型与数据对接方案》](<FEISHU_DOC_URL>)（Token: \`<DOC_TOKEN>\`）。
- **核心提取要点**：
  1. **优化目标定义**：主优化目标（压实密度 $\\text{g/cm}^3$）与硬约束指标（首次放电容量 $\\text{mAh/g}$、首次库仑效率 $\\ge 85\\%$、循环寿命）。
  2. **4 大工艺变量池**：
     - 前驱体与原材料（$\\text{Fe/P}$ 比、振实密度、BET、一次粒径）；
     - 配料与砂磨（$\\text{Li/Fe}$ 比、碳源类型与添加量、砂磨细度 D50/D100、砂磨线速度/时间）；
     - 喷雾造粒与高温烧结（进/出风温、烧结主温区温度、恒温保温时间、残氧量 $\\text{ppm}$、升降温速率）；
     - 成品粉体物性与微观表征（总碳 $\\text{wt}\\%$、成品 BET、D10/50/90、残碱、包覆层厚度）。
  3. **数据质量五步门禁**：工况对齐、全流程批次号关联链条、装配异常样本剔除、缺失值物理插补、历史有效样本量预期（30~50 批次）。

---

## 二、 怎么写（按项目生命周期 5 阶段演进模版）

工艺优化的双周/周报不能写死，必须**根据当前所属的项目生命周期阶段**灵活套用以下模板：

### 核心叙事法则（Anti-AI 铁律）
1. **真实硬核**：直接说具体参数、物理量、样本数、真实 Gap 与阻塞点，禁止空洞大话。
2. **三要素闭环**：每一期必须交代 **① 技术产出** + **② 实测数据/指标** + **③ 协同与阻塞项**。

---

### 阶段 1：需求对齐与数据对接期（当前阶段）

> **阶段特征**：刚启动项目，明确目标、圈定变量池、制定门禁标准、与客户对齐字段。

\`\`\`markdown
当前进展：
1. 编制完成《目标材料专题模型与数据对接方案》（关联飞书文档 cite），明确以压实密度为主优化目标，容量、首效、循环等作为约束指标。
2. 梳理前驱体物性、配料砂磨、造粒烧结、成品表征等 4 类关键变量池，并确立数据质量五步门禁（工况对齐、全流程批次链条关联、异常剔除、缺失插补规则）。
3. 与客户技术专家（客户技术专家）完成首轮需求与关键字段沟通，推进字段范围与历史台账对接。

下一步计划：
1. 推动客户完成核心字段与取值范围确认，收集 30~50 组具备完整工艺记录的历史批次台账。
2. 开展首批 LFP 样本数据质量评估，跑通清洗管道并检查批次追溯链条与异常分布。
3. 形成目标材料工艺优化模型算法需求规格文档，准备组织需求评审会。
\`\`\`

---

### 阶段 2：数据清洗、EDA 与特征工程期

> **阶段特征**：拿到客户历史数据，跑清洗流水线，发现脏数据/断链/异常，进行探索性分析。

\`\`\`markdown
当前进展：
1. 接收客户首批交付的 XX 组 LFP 历史批次数据，完成数据字典对齐与格式标准化。
2. 跑通数据质量五步门禁：发现 X 组批次存在编号断链（烧结炉次缺失）、X 组扣电首效<80%异常剔除，最终锁定 XX 组有效建模样本。
3. 完成特征探索分析（EDA）：发现细磨 D50 与总碳含量对压实密度的相关性最高，提取配料摩尔比与烧结温区积分等衍生特征。

下一步计划：
1. 针对次要缺失变量采用物理先验约束插补，完成训练集/测试集划分（如 4:1 按时间划分）。
2. 构建工艺优化基线模型（Baseline），完成第一轮离线回归精度评测。
3. 与客户专家复核离群样本的工艺异常记录。
\`\`\`

---

### 阶段 3：算法建模、Baseline 与离线评测期

> **阶段特征**：多模型对比、贝叶斯优化（BO）框架搭建、超参调优、特征重要性排序。

\`\`\`markdown
当前进展：
1. 完成 GP / XGBoost / 混合机理模型 Baseline 搭建，在测试集上压实密度预测 RMSE 达到 X.XX g/cm³（R²=X.XX）。
2. 完成特征贡献度归因（SHAP 分析）：确认主烧结温度在 XXX~XXX℃ 区间存在明显非线性极值，细磨 D50 最佳窗口在 X.X~X.X μm。
3. 完成高维贝叶斯优化（BO）推荐算法框架开发，集成多目标约束（容量>XXX mAh/g）。

下一步计划：
1. 优化采集函数（Acquisition Function），增强边界勘探能力与抗噪鲁棒性。
2. 生成第 1 轮目标材料推荐工艺候选参数方案（Top 3~5 组）。
3. 组织内部算法评审，输出候选参数置信度与机理合理性评估报告。
\`\`\`

---

### 阶段 4：候选方案推荐与产线闭环验证期

> **阶段特征**：给出推荐方案，客户实验/产线排产验证，实测数据回填，归因分析。

\`\`\`markdown
当前进展：
1. 完成第 X 轮工艺推荐，向客户输出 3 组候选方案（涵盖推荐配方、细磨参数与烧结温度曲线），给出预期压实密度预测区间 [X.XX ~ X.XX] g/cm³。
2. 协同客户完成试制排产与扣电测试，实测压实密度达到 X.XX g/cm³（相比基线提升 X.X%），容量与首效均满足约束。
3. 开展实测与预测 Gap 归因：第 2 组因实际炉温漂移 5℃ 导致预测偏差 X%，已将扰动因子纳入鲁棒性修正。

下一步计划：
1. 将本轮实测批次数据回填至暖启动知识库，更新先验分布。
2. 启动第 X+1 轮候选工艺推荐，针对高压密极限参数区间进一步收敛。
3. 准备实验对比分析报告与产线阶段性进展汇报。
\`\`\`

---

### 阶段 5：模型迭代、工具化与交付验收期

> **阶段特征**：算法固化、接口集成、性能压测、验收文档与培训。

\`\`\`markdown
当前进展：
1. 完成 LFP 工艺推荐核心算法封装，提供标准的配方推荐、物性预测与参数敏感性分析 API 接口。
2. 配合平台研发完成算法模块接入与前端交互联调，跑通批量计算与报告自动导出链路。
3. 整理一期算法交付测试报告，核心指标（压实密度提升幅度和推荐准确率）达到合同验收基线。

下一步计划：
1. 配合进行系统试运行与客户培训，收集试运行反馈并修复边缘 Bug。
2. 完善算法技术白皮书与交付归档文档。
3. 推进一期项目终验评审。
\`\`\`

---

## 三、 飞书文档更新标准操作流（CLI）

当根据上述模板整理好内容后，使用 \`lark-cli\` 执行局部更新：

\`\`\`bash
# 1. 精确定位例会文档中的 Block ID
lark-cli docs +fetch --doc "<DOC_TOKEN>" --detail with-ids --as user

# 2. 更新第 3 节「当前进展」下的工艺优化
lark-cli docs +update --doc "<DOC_TOKEN>" --command block_replace --block-id "<BLOCK_ID_3>" \\
  --content '<ol><li seq="1">...</li><li>...</li><li>...</li></ol>' --doc-format xml --as user

# 3. 重新 fetch 获取第 4 节最新 Block ID 并更新「下一步计划」
lark-cli docs +update --doc "<DOC_TOKEN>" --command block_replace --block-id "<BLOCK_ID_4>" \\
  --content '<ol><li seq="1">...</li><li>...</li><li>...</li></ol>' --doc-format xml --as user

# 4. 闭环验证
lark-cli docs +fetch --doc "<DOC_TOKEN>" --as user
\`\`\`
`;

const FEISHU_FIND_SKILL = `---
name: feishu-find-skill
version: 1.0.0
license: MIT
description: "在 Feishu Agent 内搜索、审阅并安装第三方 Skill。当用户说查找 skill、推荐 skill、安装 skill、/find-skill，或想把普通 Pi skill 放进 Feishu 时使用。"
---

# Feishu Skill Discovery

使用内置的 \`/find-skill\` 命令发现和安装第三方 Skill；不要直接调用普通 Pi 的全局安装路径。

## 用法

- 搜索：\`/find-skill <关键词>\`
- 从搜索结果选择后，Feishu Agent 会显示 source、安装量、\`SKILL.md\` 中声明的描述/许可证和目标路径，再询问是否安装；安装量只是排序信号，不是安全或法律背书。
- 明确安装某个结果：\`/find-skill install <owner/repo@skill-name>\`

## 边界

- 目标只允许是 Feishu Agent 私有目录：\`~/.feishu-agent/skills/<name>\`；不要写入 \`~/.agents/skills\`、\`~/.pi/agent/skills\` 或项目的 \`.agents/.pi\`。
- 启动和 \`feishu init\` 不搜索、不自动安装；联网只发生在用户显式调用 \`/find-skill\` 时。
- 第三方 Skill 以当前用户权限运行。许可证缺失或内容未经审阅时要明确提示，不把“可安装”说成“已合规”。
- 安装完成后重新加载 Runtime；如果当前模式没有 UI，搜索可以输出结果，但安装必须快速失败，不能等待确认。
`;

const DESLOP_ZH = `---
name: deslop-zh
description: 去除中文写作中的 AI 腔调，让文本像人写的而非模型生成的。覆盖博客、技术文档、推文、发行说明、科学论文等场景。触发词：去 AI 味、改写、润色、审稿、翻译腔重、像人写的、自然一点。英文去 AI 味见 deslop-en
metadata:
  trigger: 去AI味, 改写, 润色, 写一段, 审稿, 改稿, 翻译腔, 自然一点, 像人写的, 技术文档, 博客, 推文, 发行说明, release notes, 文章
  sources:
    - https://github.com/hexiecs/talk-normal
    - https://github.com/ninehills/skills (write, tech-doc-style-chinese)
    - https://zh.wikipedia.org/wiki/Wikipedia:AI生成文的特徵
    - https://github.com/hardikpandya/stop-slop
    - https://github.com/stephenturner/skill-deslop
    - https://tropes.fyi
    - https://github.com/coderjatin/anti-slop-writing
---

# 中文去 AI 味写作指南

## 一、核心原则

### 1. 自然 > 风格化
最高优先级：**读起来自然，而非读起来像「努力装人」**。
- 不要为了去 AI 味而硬塞口语词（「说白了」「居然」「谁能想到」「太太太」）
- 不要为「更像人」把技术文改得像口播稿
- 如果原句已经自然、准确、清楚，不改

### 2. 减法优先
去 AI 味是做减法，不是做加法：
- 先删：解释腔、总结腔、多余转折、套话填充
- 再调：句式节奏、标点、用词口语化程度
- 不改：代码字面量、URL、API 路径、JSON 键名、数据库字段名

### 3. 保语义 > 去 AI 味
先确认事实、逻辑、因果不变。为「更口语」改坏原意 = 失败改写。

### 4. 场景决定策略
不同写作目标有不同标准：
- **技术文档**：克制、准确、可扫读，不用第二人称
- **博客/技术长文**：自然稳重的工程师口吻，不做报告腔也不做口播腔
- **社交媒体**：有性格，有立场，不列功能清单
- **发行说明**：一句话说用户效果，条目密度匹配项目风格
- **科学论文**：保持学术正式度但不用空洞套话

---

## 二、速查清单（写完后过一遍）

- [ ] 段末有「这说明」「可以看出」「到这里」开头的收尾总结句？→ 删
- [ ] 有「不是...而是...」对比句式？→ 只说结论
- [ ] 有「随着...的发展」「在当今...时代」开篇套话？→ 删
- [ ] 有「赋能」「抓手」「闭环」「拉通」「沉淀」等黑话？→ 替换
- [ ] 用了破折号（—）？→ 逗号或句号替代
- [ ] 有四五个短句连发像打电报？→ 合并或变节奏
- [ ] 章节间有「上面这些讲的是...下面我们看...」过渡句？→ 删
- [ ] 中文和英文/数字之间没加空格？→ 加
- [ ] 用了「非常」「极其」「值得注意的是」「综上所述」「例如」？→ 换成「很」「比如」
- [ ] 有升华句（把技术观察上升到人生道理）？→ 删
- [ ] 用了第二人称「你」「您」？→ 技术文档应避免；博客可酌情
- [ ] 有「一句话总结」「简而言之」等总结标签？→ 直接说结论
- [ ] 无生命事物做了人类动词（「决策浮现」「数据告诉我们」）？→ 命名真人
- [ ] 滥用三连排比？→ 换二连或一连
- [ ] 自问自答？→ 直接陈述
- [ ] 每个列表项都是 bold 关键词开头？→ 换格式

---

## 三、中文 AI 味模式

### 3.1 段末收尾总结句（最高频，优先检查）
一段刚解释完某机制，结尾再重述一遍。

**信号词**：这说明 / 到这里 / 可以看出 / 这本身就是 / 通过以上分析 / 由此可见

> ❌ 「到这里，我们已经看到了内存分配器在 allocation 路径上的三个关键优化点。」
> ✅ 直接删掉，上一句已经说完。

### 3.2 升华句
把具体工程观察上升到普适人生道理。

> ❌ 「这其实和产品设计一样：好的抽象不是为了炫技，而是为了让使用者少犯错。」
> ✅ 直接给具体建议：「所以设计抽象时，先用调用方的视角写一遍调用代码，再决定接口长什么样。」

### 3.3 对比句式（「不是...而是...」）—— 硬约束
**禁止用否定式对比结构**。任何形式的「不是 X，而是 Y」「不是 X，是 Y」「X 本身没有价值，真正有用的是 Y」「X 已经不是瓶颈，Y 才是」全部重写。

> ❌ 「真正的创新者不是有创意的人，而是五种特质同时拉满的人。」
> ✅ 「真正的创新者是五种特质同时拉满的人。」

> ❌ 「这更像创始人筛选框架，不是交易信号。」
> ✅ 「这是一个创始人筛选框架。」

> ❌ 「性能瓶颈已经不是 I/O，而是锁竞争。」
> ✅ 「锁竞争现在是主要性能瓶颈。」

### 3.4 讲解腔 / 训人感起手
> ❌ 「真拆开看...」「这背后是同一个变化...」「真正关键的问题是...」
> ❌ 「先问问是不是」「你要先明白」
> ✅ 直接说。「先确认...」「可以先看...」

### 3.5 总结腔标签
不要在结尾前加总结标签。

> ❌ 一句话总结 / 简而言之 / 概括来说 / 总而言之 / 综上所述 / 总结一下
> ❌ 一句话落地 / 一句话讲 / 一句话概括
> ✅ 直接给 punchline，不加标签。

### 3.6 文章开篇套话
> ❌ 「随着 AI 技术的不断发展...」
> ❌ 「在当今数字化浪潮中...」
> ❌ 「近年来，XXX 受到了越来越多的关注...」
> ✅ 直接开头。「用了不到一个月后...」「先说结论...」「这次想把 X 这条链路讲清楚...」

### 3.7 中文黑话 / 互联网黑话
以下词默认不用（除非引用原文或行业固定术语）：
- **绝对禁用**：赋能、抓手、闭环、沉淀、对齐、对标、拉通、打通、协同、联动、洞察、赛道、心智、调性、战役、链路、势能、兜底
- **谨慎使用**：场景、生态、体系、路径、触点、卡点、布局、矩阵、颗粒度、复盘、梳理、输出、提炼

> ❌ 「通过数据沉淀赋能业务闭环」
> ✅ 「积累数据后，业务能形成完整流程」

额外警惕：
- **过度强调重要性**：对普通主题用「至关重要」「意义重大」「关键时刻」「转折点」→ 删
- **过度列举来源**：「依据多家媒体报道...」「显示其于产业中具一定能见度」→ 删
- **表面分析附加**：用分词短语附加空洞评论，如「反映了当代中国社会的变迁」→ 删或给具体证据
- **推广语气**：「文化遗产」「文化底蕴」「自然之美」→ 删
- **模糊归因**：「据业内人士指出」「有评论认为」→ 要么说清谁说的，要么不说
- **展望章节套话**：「尽管（主题）有其意义，但其面临的挑战有...」→ 具体说问题，不给虚无展望

### 3.8 用词去正式化
| 不用 | 用 |
|------|-----|
| 非常 / 极其 | 很 |
| 值得注意的是 | （直接说结论） |
| 综上所述 | （直接收尾） |
| 例如 | 比如 |
| 购买 / 使用 | 买 / 用 |
| 很多同学 / 不少同学 | 很多人 / 不少人 |
| 这几个事 | 这几件事 |

### 3.9 标点规则
- 使用直角引号 \`「」\`，不用弯引号 \`""\`
- 禁止破折号（—），用逗号或分号替代
- 不用感叹号（除非推文/社交场景且确有情绪）
- 中文标点：，。；：！？、\`「」\`『』【】
- 四五个句号连发 = 打电报 → 合成长句或用逗号连
- 不用 em dash（—），用逗号或句号替代

### 3.10 中英混排留白
- 中文和英文之间加空格：\`CN 文字 EN\` → \`CN 文字 EN\`
- 中文和数字之间加空格：\`版本 2.0\`
- 不加空格的对象：代码块、JSON 键名、URL、内联代码

### 3.11 第二人称
- **技术文档**：禁用「你」「您」。用「开发者」「技术负责人」等角色词，或不直接称呼读者
- **博客**：可以用「你」，但不要通篇「你会发现」「你需要」「你可以看到」

### 3.12 翻译腔套路
> ❌ 「接住」「击穿」「锋利」「不崩」「不爆」「打穿」「扛住」等物理动作动词用在了抽象认知上
> ✅ 换成日常中文：「你这几条我都收到了」「这个假设不成立」

> ❌ 抽象名词做主语：\`工程上的现实比这些数字难看\`
> ✅ 重写，人/动作做主语。

### 3.13 空泛形容词预判
> ❌ 「更干净：xxx」「逻辑很清晰：xxx」
> ✅ 删掉形容词预判，直接说 xxx。

### 3.14 句式节奏
- 三连排比用太多？→ 换二连或一连。一个三连可以，背靠背的三连是模式识别失败
- 用「首先...其次...最后」？→ 换「一方面...另一方面...」
- 段落结尾都是 punchy 短句？→ 换节奏
- bold 小标题 + 句号模式（\`**xxx**。content\`）→ 改为 \`**xxx**，content\`，让 bold 变成承重词而非独立标题

---

## 四、分场景策略

### 4.1 技术文档 / API 文档 / 产品文案
**触发**：「技术文档」「API 文档」「产品文案」「界面文案」「帮助文档」

- 语气：克制、直接、可扫读
- 不用第二人称（「你」「您」）
- 不用「领先」「强大」「重磅」「颠覆」「震惊」「炸裂」等宣传形容词
- 不用「赋能」「抓手」「闭环」等黑话
- 不用感叹号
- 直角引号 \`「」\`
- 中英留白
- 不改代码字面量、JSON 键名、URL、API 路径
- 术语首次出现标注，后续不重复解释
- 不用「Hello」「Hi」等问候开篇
- 目标：说清楚「它是什么、用来做什么、下一步看哪里」

### 4.2 技术博客 / 长文
**触发**：「博客」「技术文章」「技术长文」「写一篇」「科普」

- 默认模式：资深工程师给同事讲系统的口吻
- 可以口语，但不俚语化
- 减少情绪词、感叹词、口头禅
- 少用「说白了 / 其实 / 你会发现 / 谁能想到」推进行文
- 优先追求：清楚、稳、具体、节奏自然
- 优先做减法：删解释腔、删总结腔、删多余转折
- 把润色做成减法，不是重写
- 如果原句已经自然，不改
- 非工程师受众：先去「不懂技术的同学」等居高临下词，去「你必须」等训人腔，去太深术语。走「我怎么用的，你试试」路径，不走「系统综述 + 框架表 + 长分析段落」

### 4.3 社交媒体 / 推文
**触发**：「推文」「X推文」「twitter」「社交发文」「post」

五规则：
1. **社区先行**：开头用社区锚点（star 数、感谢用户、谁的反馈推动），改动清单放后面
2. **亮点不全量**：挑 2-4 个最有意思的，读者要故事不是 changelog
3. **用户感受帧**：写法用「你用它的时候...」「有一种...的感觉」，不是「这个工具做了...」
4. **一条立场**：至少一句表明决策原因的意见句
5. **中文节奏**：用地道表达，避免翻译腔和正式词

- 结尾用邀请，不用 CTA
- 身份和敏感信号脱敏
- 不踩竞品
- 公开前扫三件事：脱敏、不踩竞品、用户感受先于功能清单

### 4.4 发行说明 / Release Notes
**触发**：「release」「changelog」「版本」「发行说明」

- 结构：Breaking Changes → New Features → Fixes & Improvements → Deprecations
- 格式：优先匹配目标项目最近一次 release 风格
- 条目：5-8 条，每条约一句话，只说用户效果
- title：版本号 + 最核心改动或主题，不超过 10 个词
- 不要加 emoji（除非项目本身庆祝风格）
- 不加「不再更新」「final release」「停止维护」等终止信号（除非真实情况）
- 中英双语：条目一一对应，不混写，英文在前

### 4.5 科学论文 / 学术写作
**触发**：「论文」「abstract」「cover letter」「grant」「manuscript」

- 保持学术正式度，但删除空洞套话
- 用「我们」指自己工作（不用被动躲闪）
- 引用具体作者，不用「有研究者指出」
- 不用「长期以来人们认识到...」
- 给出具体数字而非空泛结论
- 不用「在当今日新月异的领域中」
- 不用「这一范式转变具有深远意义」
- 不用「结果表明」「据观察」

### 4.6 双语内容
**触发**：「双语」「中英」「bilingual」「翻译」

- 中英字符间加空格
- 不混用标点：中文用中文标点，英文用英文标点
- 术语跨全文一致
- 术语首次出现：保留英文 + 中文注解（或中文前置 + 英文括注）
- 后续只用中文，不要中英混用
- 常见可替换：context→上下文、state→状态、cache→缓存、claim→断言、runtime→运行时
- 术语未收敛的（prompt、embedding、tokenizer）保留英文合理
- 双语版：条目编号一一对应，确认 EN/CN 意思一致，标记翻译损失
- 禁止破折号

---

## 五、词汇替换表（正式 → 口语）

| 不用 | 用 |
|------|-----|
| 非常 / 极其 | 很 |
| 值得注意的是 | 直接说结论 |
| 综上所述 / 总之 | 直接收尾 |
| 例如 | 比如 |
| 购买 | 买 |
| 使用 | 用 |
| 拥有 | 有 |
| 呈现 | 是 / 显得 |
| 进行（+动词）| 删掉「进行」，用动词本身 |
| 赋能 | 提供 / 帮助 |
| 抓手 | 关键措施 |
| 闭环 | 完整流程 |
| 沉淀 | 形成 / 积累 |
| 对齐 | 统一 |
| 拉通 / 打通 | 连接 / 贯通 |
| 洞察 | 分析结论 |
| 兜底 | 保障机制 |
| 至关重要 / 意义重大 | 重要 |
| 褒有 | 有 |
| 坐落于 | 在 |
| 致力于 | 努力 / 专注 |

---

## 六、执行流程

1. **先识别场景**：博客？技术文档？推文？论文？确定语气标准。
2. **先保语义**：确认不被改坏。
3. **做减法**：
   - 先扫段末总结句（中文最高频 AI 痕迹）
   - 再扫对比句式（「不是...而是...」）
   - 再扫喉清开场、元评论、强调拐杖
   - 再扫副词、被动语态、假主体
   - 再扫黑话、空洞形容词
4. **再顺句子**：节奏、标点、中英留白、破折号
5. **最后回读**：优先修不自然处，不强改自然处
6. **输出**：只给改写后的文本。不加改动清单、解释、收尾话。除非用户明确要求。

---

## 七、改写示例

### 示例 1：中文技术博客

**改写前：**
> 众所周知，随着云原生技术的不断发展，Kubernetes 已经成为了容器编排领域的事实标准。值得注意的是，虽然 K8s 本身功能强大，但在实际落地过程中，很多同学会发现它并不是银弹。本文将从多个维度深入探讨企业在落地 Kubernetes 时面临的挑战和应对策略，希望能为正在考虑或已经踏上云原生之旅的各位带来一些启发。

**改写后：**
> 我们团队用 Kubernetes 跑了三年生产环境。最大的感受：K8s 解决了部署问题，但把复杂度转移到了别的地方。这篇文章记录我们踩过的五个坑，以及每个坑的止损方案。

**改动点**：删了开篇套话、删了「值得注意的是」「很多同学」、删了对比句式「不是银弹」、删了「从多个维度深入探讨」、删了升华式结尾、换成了具体事实和个人经验。

### 示例 2：技术文档

**改写前：**
> 你好！欢迎使用我们的 API 服务！我们的平台提供领先的、强大的数据处理能力，能够赋能您的业务，助力您在数字化转型的赛道上实现弯道超车。使用前，您需要先进行 API Key 的申请。

**改写后：**
> API 提供以下数据处理能力：批量查询、实时推送、数据清洗。调用前需先申请 API Key，详见「认证」章节。

**改动点**：删了问候语、删了感叹号、删了宣传形容词+黑话链条、删了第二人称、把废话换成了功能清单。

### 示例 3：对比句式修复

**改写前：**
> 微服务架构的价值不在"微"，而在于边界清晰。

**改写后：**
> 微服务架构的价值在于边界清晰。

### 示例 4：空洞套话净化

**改写前：**
> 从多个维度来看，Redis 在高并发场景下具有至关重要的意义。不仅能够提升系统响应速度，还对整体架构的可扩展性产生了深远影响。

**改写后：**
> Redis 用单线程模型处理读写，省去了锁开销。我们压测发现，相同硬件下 Redis 的 QPS 比 MySQL 高两个数量级。

---

## 八、质量评分（可选用）

五个维度，每维 1-10 分，低于 35/50 需修订：

| 维度 | 问题 |
|------|------|
| 直接性 | 是在陈述事实还是在预告/总结？ |
| 节奏 | 句子长短有变化还是千篇一律？ |
| 信任 | 有没有低估读者智商（解释过度、灌输腔）？ |
| 真实度 | 读起来像人写的还是像 AI 生成的？ |
| 密度 | 还有能删的吗？ |

---

## 九、相关技能

- \`tech-doc-style-chinese\`：中文技术文档排版专项（直角引号、中英留白、黑话规避）
- \`deslop-en\`：英文去 AI 味写作（喉清开场、副词、假主体、对比句式、AI 词汇禁表等）
`;

const FEISHU_PRO_DIAGRAM = `---
name: feishu-pro-diagram
version: 1.0.0
description: "飞书专业工程图表与高颜值画板设计指南。专为技术文档、述职报告、系统架构、时序交互等场景设计，解决 Mermaid 默认惨白空旷、复杂 SVG 降级白屏、以及 DSL 自动绕线连线重叠等痛点。"
metadata:
  trigger: 飞书画板, 画板美化, 架构图, 时序图, 流程图, 泳道图, SVG画板, 画板设计
---

# 飞书专业工程画板（Whiteboard）设计指南

本指南是飞书画板从“能看”到“企业级高颜值”的实战总结，用于指导 Agent 在飞书云文档中绘制高密度、无重叠、不降级、具备设计美感的专业图表。

---

## 一、画板类型分流铁律（选型决策）

| 图表类型 | 首选格式 | 核心规范与避坑原则 |
|---|---|---|
| **系统架构图 / 全景拓扑图** | **SVG 画板** (\`type="svg"\`) | **必须横向多列卡片布局**，固定 \`viewBox\`，使用卡片渐变底、细边框、\`feDropShadow\` 悬浮阴影、顶部药丸徽章（Pill Badge）。信息内聚在卡片中。 |
| **阶段演进 / 路线图 (Roadmap)** | **SVG 画板** (\`type="svg"\`) | 采用分色阶段卡（已完成绿、推进中橙、规划中灰），底部单条虚线回流，禁止纵横交错的多重连线。 |
| **多角色交互时序图 / 泳道流程** | **原生 DSL 布局** 或 **Mermaid sequenceDiagram** | **绝对禁令：严禁在连线上写文字（\`label\`）！** 连线文字必压盖节点。所有步骤说明（如“①输入背景”、“②提交求解”）必须写入卡片文本内部，连线只保留纯净单向箭头。 |

> ⚠️ **致命坑点警告（必须严格遵守）**：
> 1. **严禁使用复杂 SVG 模拟带虚线生命线、透明覆盖层的时序图**：飞书服务端 \`svg-parser\` 解析多层重叠与虚线生命线时极易失败，直接降级为“加载占位图标（白屏小图标）”。
> 2. **严禁全篇纯白底色加单层线框**：默认未设置背景的 Mermaid/SVG 会在 2560px 画布上留下 80% 惨白虚空，视觉极差。

---

## 二、四大语义色板规范（强制套用）

画板禁止随意使用饱和度极高的原色（如纯红、纯绿），必须采用现代化软色调配对：

| 角色 / 分层 | 容器底色 (\`fillColor\`) | 边框主色 (\`borderColor\`) | 标题/文字主色 (\`textColor\`) | 药丸徽章底色 (\`badgeBg\`) | 徽章文字色 (\`badgeText\`) |
|---|---|---|---|---|---|
| **专家 / 业务输入层** | \`#FFFDF5\` / \`#FFF7ED\` | \`#F59E0B\` (Amber) | \`#92400E\` / \`#78350F\` | \`#FEF3C7\` | \`#B45309\` |
| **Agent / 编排调度层** | \`#F0F7FF\` / \`#EFF6FF\` | \`#3B82F6\` (Blue) | \`#1E40AF\` / \`#1E3A8A\` | \`#DBEAFE\` | \`#1D4ED8\` |
| **算法 / 数学求解层** | \`#FAF5FF\` | \`#A855F7\` (Purple) | \`#6B21A8\` / \`#581C87\` | \`#F3E8FF\` | \`#7E22CE\` |
| **实验验证 / 交付闭环层** | \`#F0FDF4\` / \`#ECFDF5\` | \`#10B981\` (Emerald) | \`#065F46\` / \`#064E3B\` | \`#D1FAE5\` | \`#047857\` |
| **中性连接线 / 背景** | \`#F8FAFC\` (主画板底) | \`#E2E8F0\` (外框) | \`#64748B\` (副标题) | \`#94A3B8\` (连线) | \`#FFFFFF\` |

---

## 三、内置原生图标库（Icon Assets）

在画板 DSL 中，可直接通过 \`{"type":"icon", "name":"...", "size":32, "color":"#..."}\` 插入飞书内置矢量图标，无需外链图片：

- **计算与智能**：\`chip\`（芯片/算力）、\`bot\`（智能体/Agent）、\`controller\`（控制器）、\`terminal\`（终端/代码）
- **数据与存储**：\`database\`（数据库）、\`data-display\`（看板）、\`cloud-storage\`（云存储）、\`table\`（表格）
- **业务与目标**：\`target\`（目标/Baseline）、\`factory\`（工厂/车间产线）、\`settings\`（工艺参数/配置）、\`toolbox\`（工具箱）
- **协作与人员**：\`person\`（领域专家）、\`business-user\`（业务人员）、\`comment\`（评审讨论）、\`document\`（指导书/报告）
- **状态反馈**：\`success\`（验证通过）、\`warning\`（工艺红线/预警）、\`refresh\`（迭代循环）、\`lightning\`（快速推理）

---

## 四、SVG 架构图高颜值模版（标准结构）

\`\`\`xml
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1260 520" width="1260" height="520">
  <defs>
    <!-- 卡片立体悬浮阴影 -->
    <filter id="card-shadow">
      <feDropShadow dx="0" dy="4" stdDeviation="6" flood-color="#0f172a" flood-opacity="0.06"/>
    </filter>
    <!-- 胶囊徽章阴影 -->
    <filter id="pill-shadow">
      <feDropShadow dx="0" dy="2" stdDeviation="3" flood-color="#0f172a" flood-opacity="0.05"/>
    </filter>
    <!-- 背景与卡片浅底渐变 -->
    <linearGradient id="bg-grad" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0%" stop-color="#f8fafc"/><stop offset="100%" stop-color="#f1f5f9"/>
    </linearGradient>
    <linearGradient id="card-blue" x1="0" y1="0" x2="0" y2="1">
      <stop offset="0%" stop-color="#ffffff"/><stop offset="100%" stop-color="#f0f7ff"/>
    </linearGradient>
    <!-- 正交连接箭头 -->
    <marker id="arrow" viewBox="0 0 10 10" refX="6" refY="5" markerWidth="6" markerHeight="6" orient="auto-start-reverse">
      <path d="M 1 2 L 7 5 L 1 8 z" fill="#94a3b8"/>
    </marker>
  </defs>

  <!-- 1. 外层全局画板容器（锁定紧凑比例，杜绝边缘大片留白） -->
  <rect x="0" y="0" width="1260" height="520" rx="16" fill="url(#bg-grad)" stroke="#e2e8f0" stroke-width="1"/>

  <!-- 2. 顶部主标题与元数据药丸徽章 -->
  <g transform="translate(36, 28)">
    <rect x="0" y="0" width="96" height="24" rx="12" fill="#2563eb" filter="url(#pill-shadow)"/>
    <text x="48" y="16" fill="#ffffff" font-size="11" font-weight="700" text-anchor="middle">ARCHITECTURE</text>
    <text x="110" y="17" fill="#0f172a" font-size="16" font-weight="700">系统总体架构名称</text>
    <text x="360" y="17" fill="#64748b" font-size="13">模块划分 · 核心求解机制 · 现场工程闭环</text>
  </g>

  <!-- 3. 卡片间正交平直箭头 -->
  <line x1="250" y1="230" x2="274" y2="230" stroke="#94a3b8" stroke-width="2" marker-end="url(#arrow)"/>

  <!-- 4. 垂直分层信息卡片（内嵌子节点与细实线分割） -->
  <g transform="translate(36, 80)" filter="url(#card-shadow)">
    <rect x="0" y="0" width="214" height="320" rx="12" fill="url(#card-blue)" stroke="#3b82f6" stroke-width="1.5"/>
    <rect x="14" y="14" width="70" height="20" rx="10" fill="#dbeafe"/>
    <text x="49" y="28" fill="#1d4ed8" font-size="10" font-weight="700" text-anchor="middle">LAYER 01</text>
    <text x="14" y="58" fill="#1e40af" font-size="15" font-weight="700">分层名称</text>
    <line x1="14" y1="90" x2="200" y2="90" stroke="#bfdbfe" stroke-width="1"/>

    <!-- 子条目卡片 -->
    <rect x="12" y="104" width="190" height="60" rx="8" fill="#ffffff" stroke="#dbeafe" stroke-width="1"/>
    <text x="22" y="124" fill="#1e3a8a" font-size="12" font-weight="700">关键对象/参数</text>
    <text x="22" y="142" fill="#3b82f6" font-size="11">• 具体的工业工程细节</text>
  </g>

  <!-- 5. 底部反馈虚线（平滑圆弧导角 Q，带中央药丸提示） -->
  <path d="M 1100 420 L 1100 460 Q 1100 475 1080 475 L 140 475 Q 120 475 120 460 L 120 420"
        fill="none" stroke="#2563eb" stroke-width="2" stroke-dasharray="6,4" marker-end="url(#arrow)"/>
  <g transform="translate(610, 475)">
    <rect x="-140" y="-12" width="280" height="24" rx="12" fill="#ffffff" stroke="#93c5fd" stroke-width="1" filter="url(#pill-shadow)"/>
    <text x="0" y="4" fill="#1d4ed8" font-size="11" font-weight="600" text-anchor="middle">实验反馈与数据回填 · 驱动下一轮优化迭代</text>
  </g>
</svg>
\`\`\`

---

## 五、DSL 时序图（防重叠布局标准）

\`\`\`json
{
  "version": 2,
  "nodes": [
    {
      "type": "frame",
      "id": "lanes-root",
      "layout": "vertical",
      "gap": 28,
      "children": [
        {
          "type": "frame",
          "id": "lane-1",
          "layout": "horizontal",
          "height": 180,
          "gap": 32,
          "padding": 20,
          "fillColor": "#FFFDF5",
          "borderColor": "#F59E0B",
          "borderWidth": 2,
          "borderRadius": 12,
          "children": [
            { "type": "text", "id": "l1-title", "text": "角色 A", "width": 110, "fontSize": 17, "fontWeight": "bold", "textColor": "#B45309" },
            { "type": "frame", "id": "cell-1", "width": 300, "height": 140, "layout": "vertical", "alignItems": "center", "justifyContent": "center",
              "children": [
                { "type": "rect", "id": "node-1", "width": 280, "height": "fit-content", "fillColor": "#FEF3C7", "borderColor": "#F59E0B", "borderWidth": 2, "borderRadius": 10, "text": "① 输入背景与工艺目标\\n明确自变量 X 与工艺红线", "fontSize": 13, "textColor": "#78350F", "padding": 12 }
              ]
            }
          ]
        }
      ]
    },
    {
      "type": "connector",
      "connector": {
        "from": "node-1",
        "to": "node-2",
        "lineShape": "rightAngle",
        "lineColor": "#94A3B8",
        "lineWidth": 2,
        "endArrow": "arrow"
      }
    }
  ]
}
\`\`\`
**关键规则**：连线 \`connector\` 严禁写 \`label\`，说明直接写在 \`rect.text\` 内部。
`;

const FEISHU_PACKAGE_CURATOR = `---
name: feishu-package-curator
description: 为 Feishu Agent（feishu 命令）搜索、筛选和安装 Pi 社区插件包（Package），按 Feishu 边界规范执行并做装后验证。当用户想给 feishu 装插件/扩展/主题/skill 包，问"有没有好用的 pi 插件""帮我装个 xxx 插件""feishu 装扩展"时使用。不适用于给普通 Pi（pi 命令）装包。
metadata:
  trigger: 装插件, 安装插件, 装扩展, pi插件, pi包, pi package, feishu install, 插件推荐, 找插件, 主题包, 扩展包, 装主题
---

# Feishu Package Curator

帮用户给 Feishu Agent 装 Pi 社区包。这是流程规范，不是安全沙箱——核心工具保护和资源隔离由 feishu 运行时强制，本 skill 只负责"选得对、装得稳、有记录"。

## 铁律

1. **唯一安装入口是 \`feishu install\`**。绝不运行 \`pi install\`、\`pi update --extensions\`，不手拷文件到 \`~/.pi/\` 或 \`~/.feishu-agent/\`。feishu 的包在 \`~/.feishu-agent/\`，与普通 Pi 完全隔离。
2. **必须钉精确版本**：\`feishu install npm:<name>@<exact-version>\`。
   - npm 包：\`npm view <name> version\` 取最新版后钉住。
   - Git 包：\`feishu install git:github.com/<user>/<repo>\`（包管理器会钉 commit 引用）。
   - 用户只是想试用、明确说"不钉"时可以例外，但要在台账里记成 unpinned。
3. **装后 \`/reload\`**：装完或更新完包，提醒用户在当前 feishu TUI 会话里运行 \`/reload\`（资源重载，不用重启）。若 feishu 本体（pi-coding-agent）升级则必须重启进程。

## 筛选规范（轻量建议，不是硬门禁）

搜索来源：[BubblePtr/awesome-pi](https://github.com/BubblePtr/awesome-pi)（\`https://raw.githubusercontent.com/BubblePtr/awesome-pi/main/README.md\`）、pi.dev/packages、\`npm search pi-package\`。

按 Feishu 边界分类，把判断和理由告诉用户，由用户拍板：

- ✅ **推荐装**：纯 UI 类（主题包、status line、通知、趣味短语、/btw 这类侧边命令）、后台进程类、LSP/代码智能类、浏览器/搜索工具类。只用标准扩展 API，不与核心保护层冲突。
- ⚠️ **逐个试**：子代理编排、plan mode、todo/任务管理、交互式 shell。能用，但要装后烟测。
- ⛔ **建议不装**（说明原因，用户坚持可装，装后验证）：
  - 记忆类（pi-memory、hermes-memory、gentle-engram 等）——feishu 已有 Mem0 记忆，两套记忆双重注入、身份串味。
  - 权限/沙箱/审批类（guardrails、pi-sandbox、permission-system、verdict 等）——与 feishu 内置 lark-cli 高危命令 guard 重复，会双重拦截 bash。
  - 账号/settings 同步类（pi-accounts、pi-sync）——feishu 对 \`~/.pi\` 的 auth 只读复用，写入语义对不上。
  - 替代运行时类（pi-web、pi-telegram、pi-acp 等）——自带会话模型，与 feishu Remote Bridge/会话隔离冲突。

## 安装流程

1. **确认来源和版本**：\`npm view <name> version\`（或查 GitHub release），把包名、版本、一句话作用、分类（推荐/试装/不建议）报给用户，等用户确认再装。
2. **安装**：\`feishu install npm:<name>@<version>\`（项目级加 \`-l\`，默认全局）。
3. **登记确认**：\`feishu list\` 能看到该包。
4. **装后验证**：
   - 跑一轮 print 烟测抓启动告警：\`feishu -p "reply ok" 2>&1 | grep -i "startup warning"\`。有输出就把 warning 原文给用户看——核心工具/保留命令冲突会在这里显式报出，不会静默。
   - 或让用户 \`/reload\` 后直接看 TUI 启动诊断。
5. **出问题不自动处置**，给用户两个选项：\`feishu config set npm:<name>@<version> extensions off\`（只关扩展，主题/skill 资源保留）或 \`feishu remove npm:<name>@<version>\`。
6. **记账**：往 \`~/.feishu-agent/package-ledger.md\` 追加一行表格记录（文件不存在就建，带表头）：日期、包源（含版本）、分类、烟测结果（clean/warnings 摘要）。示例：
   \`| 2026-09-xx | npm:pi-kanagawa@1.2.0 | UI主题 | clean |\`

## 更新

- \`feishu update <source>\` 更新单个包，\`feishu update --extensions\` 更新全部。更新同样钉新版本号、跑烟测、记台账，然后 \`/reload\`。

## 注意

- 包的配置不会从普通 Pi 带过来，需要在 feishu 里重配。
- 扩展以当前用户权限运行，本 skill 不做代码审计/供应链审核；用户在意时提示生态有 \`pi-vetter\` 这类包可自行评估。
- \`feishu list\` / \`feishu config\` / \`feishu remove\` 是管理命令，不受运行时 \`PI_OFFLINE\` 影响，可以放心调。
`;

const FEISHU_TECH_NOTE_WRITER = `---
name: feishu-tech-note-writer
description: "飞书技术笔记写作与改版助手。当用户要写、改写、简化、公众号化技术笔记，分析论文/技术产品/项目方案，拆解源码和工程机制，插入官方原文图片，或把技术文章排版到飞书文档时使用。支持公众号式技术解读、工程深度技术笔记、深度技术笔记、项目成果说明和极简技术分享。"
version: "1.1.0"
license: "MIT"
---

# 飞书技术笔记写作与改版

## 目标

把技术资料转化为“读得下去、技术上不失真、工程师愿意继续往下挖”的飞书文档。默认不把技术内容简单改成宣传稿，而是通过问题拆分、具体对象、短段落、证据边界和采用建议提高可读性。

## 触发后先判断

先判断四件事：

1. 是从零写，还是改已有文档？
2. 目标是公众号式技术解读、工程深度技术笔记、深度技术笔记、项目成果说明，还是极简分享？
3. 是否需要检索官方资料、源码、论文和原文图片？
4. 是否要直接更新飞书文档？若用户未明确要求，先给本地草稿/改版方案，不直接覆盖远端文档。

默认决策：
- 已有长文 + 用户说“可读性差/参考公众号” → 公众号式技术解读；
- 用户说“参考某技术作者/源码拆解/工程机制/深度技术笔记” → 工程深度技术笔记；
- 用户说“完整调研/论文解读/保留技术细节” → 深度技术笔记；
- 用户说“项目总结/交付/成果汇报” → 项目成果说明；
- 用户说“简单讲一下/一页看懂” → 极简技术分享。

## 总体流程

\`\`\`text
读取原文/资料
  → 明确读者和文章模式
  → 检索官方资料、源码和证据
  → 先产出文章骨架
  → 技术作者写初稿
  → 技术事实编辑核验
  → 结构编辑调整问题链
  → 目标读者试读
  → 选择原文图片与图位
  → 生成飞书 XML/Markdown
  → 写入或更新飞书
  → fetch 验证正文、图片和结构
\`\`\`

## 证据规则

按以下优先级获取资料：

1. 官方博客、官方文档、官方论文、官方仓库；
2. 论文、研究者公开说明、独立实验；
3. GitHub Issues、社区讨论和用户反馈。

写作时必须区分：

- 官方声称；
- 外部证据支持；
- 作者判断；
- 尚未验证的内容。

不要把官方自报数字写成独立复现结果。涉及“最新”时先确认当前日期，并在必要时注明资料截止时间。涉及可行性时检查官方限制、已知 bug、社区反复反馈和迁移成本。

## 四种输出模式

### A. 公众号式技术解读（默认）

结构：

\`\`\`text
一句话结论
→ 为什么这个问题值得关注
→ 普通方案为什么不够
→ 技术到底怎么工作
→ 一个或几个具体案例
→ 官方结果与证据边界
→ 哪些地方不能直接相信
→ 对我们的启发
\`\`\`

写作规则：
- 开头直接抛矛盾，不写空泛行业背景；
- 小标题使用结论句；
- 每段 2—4 句，只承担一个意思；
- 术语第一次出现时用人话解释；
- 每节至少给一个例子、对比或判断；
- 通常 2—4 张图，开头最多 1 张原文截图；
- 不使用连续大图；原图放在对应内容之后；
- 默认 1500—3000 字，除非用户要求长文。

### B. 工程深度技术笔记（Text Matrix 风格）

适用于技术产品、开源框架、论文系统和 Agent 架构的工程化拆解。目标不是“写得短”，而是让普通技术读者能进入、工程师能继续往下挖。

默认结构：

\`\`\`text
核心判断
→ 项目坐标
→ 问题域：这个领域真正难在哪里
→ 坑一/坑二/坑三：具体工程问题
→ 核心机制：项目怎么解决
→ 源码/对象/架构坐标
→ 一次任务流或案例
→ Benchmark/数据解读：数字测了什么，没测什么
→ 局限与代价
→ 谁该用，谁不要急着用
→ 结尾判断
→ 参考资料
\`\`\`

写作规则：
- 开头直接给“它解决的不是 X，而是 Y”的核心判断；
- 增加轻量“项目坐标”：版本、入口、核心对象、适用场景、来源和状态；
- 不按功能菜单平铺，先按工程问题组织；
- 每个问题使用“问题是什么 → 为什么难 → 怎么解决 → 工程判断”；
- 标题必须带具体问题或判断，例如“假成功：动作执行了，不等于页面真的变了”；
- 关键机制落到具体对象、参数、目录、协议、数据流或复杂度，不只讲概念；
- 关键技术段落可以较长，但必须拆成短段落和小标题；
- 对最重要的一个机制进行深挖，不把所有章节平均写浅；
- 结果部分必须写“数字测到了什么”和“数字没有测到什么”；
- 结尾必须回答“谁该用、谁不要急着用、怎么开始”；
- 通常 3—5 张图，开头最多 1 张原文截图；
- 默认 3000—6000 字，按技术复杂度调整。

### C. 深度技术笔记

结构：

\`\`\`text
核心结论
→ 问题与背景
→ 架构/机制
→ 执行流程
→ 边界与退化
→ Benchmark/实证
→ Trade-offs
→ 局限
→ 可迁移启发
→ 术语与参考
\`\`\`

可以使用原生 \`<whiteboard>\`、表格、公式和详细指标，但不为凑结构强行加入画板。时序图默认不生成，除非它确实比文字更清楚。

### D. 项目成果说明

结构：

\`\`\`text
项目结论
→ 目标与约束
→ 技术路线
→ 本人承担的工作
→ 指标与结果
→ 交付边界
→ 未完成项与风险
→ 后续计划
\`\`\`

重点保留数据、指标、部署环境、交付物和责任边界，避免写成宣传稿。

### E. 极简技术分享

结构：

\`\`\`text
它是什么
→ 它解决什么问题
→ 怎么工作
→ 一个例子
→ 一句话判断
\`\`\`

控制在 800—1500 字，最多 2 张图。

## 多角色审稿

不必真的启动多个外部 Agent，但必须按四个角色检查：

### 技术作者
保证机制完整，不能为了好读而漏掉关键条件。

### 技术事实编辑
检查：数据来源、模型/版本、时间、官方自报口径、限制、术语和案例是否准确。

### 结构编辑
检查：文章是否按真实问题组织，标题是否带判断，核心机制是否被平均摊薄，是否有“问题—机制—判断”闭环。

### 目标读者
检查：第一次接触该主题的人能否回答“它是什么、为什么有用、怎么工作、有什么风险”。标记读者第一次掉队的位置，并指出是哪一个术语、段落或图导致的。

## 图片规则

优先级：

\`\`\`text
官方原文图/官方原文截图
  > 项目原始图表或运行截图
  > 自制架构图/流程图
  > AI 生成装饰图
\`\`\`

图位规则：

- 原文截图：开头最多 1 张，仅用于建立语境；
- 核心机制图：紧跟机制解释；
- 结果图：放在实验或案例段落后；
- 一张图只承担一个解释任务；
- 图注注明来源；
- 中等文章通常 3—4 张，长文通常不超过 5—6 张；
- 图片太长时优先裁剪或只保留关键区域；
- 连续两张图片之间必须有正文解释；
- 飞书文档中使用本地素材：\`<img path="@./assets/foo.png" width="820" caption="来源：..."/>\`。

## 飞书交付

### 读取

\`\`\`bash
lark-cli docs +fetch --doc "文档URL或token" --doc-format xml --detail with-ids --as user
\`\`\`

大文档先读取 outline 或目标 section，不要一开始就覆盖全文。

### 创建

先在当前项目目录写 XML，例如 \`./notes/article.xml\`，再执行：

\`\`\`bash
lark-cli docs +create --title "标题" --parent-token "目录token" \\
  --content "@./notes/article.xml" --doc-format xml --as user
\`\`\`

### 更新

除非用户明确要求完全重建，否则不要 overwrite。局部改写使用 \`block_replace\`；用户明确要求“按新结构重写全文”时，可以使用 \`overwrite\`，但写入后必须重新 fetch 验证。

\`\`\`bash
lark-cli docs +update --doc "DOC_TOKEN" --command block_replace \\
  --block-id "BLOCK_ID" --content "@./notes/section.xml" \\
  --doc-format xml --as user
\`\`\`

新增章节使用 \`block_insert_after\`；删除冗余内容使用 \`block_delete\`。每次写入后重新 fetch，不沿用失效的 block ID。

### 图片

本地图片可通过 XML \`<img path="@./assets/image.png"/>\` 写入。用户明确要求“直接截图原文”时，使用浏览器获取官方页面截图；默认只保留首屏和最能解释机制/结果的少量页面，不把整篇网页连续贴入文档。

## 最终检查清单

- [ ] 核心判断在开头是否说清楚？
- [ ] 是否先讲真实问题，再讲产品功能？
- [ ] 每个问题是否都有“问题—机制—判断”？
- [ ] 关键机制是否落到具体对象、参数、流程或数据？
- [ ] 标题是否表达观点，而不是只做目录？
- [ ] 段落是否过长？
- [ ] 术语第一次出现是否解释？
- [ ] 官方说法、事实、作者判断是否分开？
- [ ] 数字是否有来源和口径？
- [ ] 是否写清数字测到了什么、没测到什么？
- [ ] 是否明确限制、代价和采用边界？
- [ ] 图片是否确实帮助理解？
- [ ] 开头是否图片过多？
- [ ] 飞书图片、画板、表格是否成功渲染？
- [ ] 写入后是否重新 fetch 验证？

## 手动调用

\`\`\`text
/skill:feishu-tech-note-writer
\`\`\`

调用时可附加：

- \`模式=公众号式技术解读\`
- \`模式=工程深度技术笔记\`
- \`模式=深度技术笔记\`
- \`模式=项目成果说明\`
- \`模式=极简技术分享\`
- \`风格参考=Text Matrix 技术笔记\`
- \`图片策略=优先使用官方原文图，开头最多一张截图\`
`;

const FEISHU_TECH_NOTE_WRITER_FILES: Record<string, string> = {
  "references/evidence-check.md": `# 证据与可行性检查

## 来源分层

A 类：官方博客、官方文档、论文、仓库、发布说明。

B 类：独立论文、研究者公开说明、第三方复现实验。

C 类：GitHub Issues、社区帖子、用户反馈。

## 必查问题

- 数字是官方自报还是独立复现？
- 对照条件、模型版本、prompt、数据集是否公开？
- 是否有人类介入？
- 是否披露 token、时间、成本和失败率？
- 是否有已知 bug、配额问题、无限循环或不支持场景？
- 从原系统迁移到自己的系统，依赖哪些基础设施？

## 写作口径

官方自报应使用：

- “官方称……”
- “官方报告……”
- “按官方口径……”

证据不足时应使用：

- “目前无法独立验证……”
- “这一点仍缺少公开数据……”
- “可以作为强信号，但不能视为定论……”

禁止把“官方声称达到 71%”改写成“系统达到 71%”。
`,
  "references/image-layout.md": `# 图片选择和飞书排版

## 图片预算

- 极简分享：最多 2 张；
- 公众号式技术解读：通常 3—4 张；
- 深度技术笔记：通常 4—6 张；
- 原文页面截图：开头最多 1 张；
- 连续两张图片之间应有正文解释，不连续堆图。

## 选择顺序

1. 官方原图；
2. 官方页面截图；
3. 项目原始截图或指标图；
4. 自制流程图；
5. 装饰性图片。

## 图位

- 主图：开头，在一句话结论之后；
- 机制图：对应机制首次解释之后；
- 结果图：对应数据和案例之后；
- 原文截图：只用于建立原文语境，不替代中文解释。

## XML 示例

\`\`\`xml
<img path="@./assets/official.png" width="820" caption="来源：官方原文"/>
\`\`\`

## 画板

只有当画板比正文或官方图片更清楚时才创建。复杂架构可用一张 SVG 画板；时序图默认不创建。已有画板更新时复用原 token，不新建空白画板。
`,
  "references/templates.md": `# 文章骨架模板

## 公众号式技术解读

1. 标题：技术名词 + 核心矛盾/结果
2. 开头：一句反常识或真实问题
3. 一句话结论
4. 为什么普通方案不够
5. 技术如何工作
6. 具体案例或结果
7. 证据边界与风险
8. 我们能借鉴什么
9. 参考资料

## 深度技术笔记

1. 核心结论
2. 问题定义
3. 全景架构
4. 核心机制
5. 执行流程
6. 异常与退化
7. 实验与复现差距
8. Trade-offs
9. 局限
10. 可迁移启发
11. 术语和参考

## 项目成果说明

1. 报告结论
2. 项目背景与目标
3. 技术路线
4. 本人承担的工作
5. 技术指标与完成情况
6. 项目成果与交付边界
7. 风险与后续计划
`,
  "references/wechat-style.md": `# 公众号式技术表达规则

## 开头

不要从“随着……快速发展”开始。优先使用：

- 反常识：多个 Agent 可能一起犯错；
- 真实场景：论文看完了，但读者仍不知道它解决了什么；
- 直接冲突：模型更大，不代表系统更可靠；
- 结果先行：官方声称性能翻倍，但真正的问题是能否复现。

## 段落

- 一段只做一件事；
- 2—4 句为宜；
- 长解释拆成“结论—解释—例子”；
- 连续出现三个以上术语时，插入一句人话总结；
- 重要判断单独成段。

## 标题

标题应表达本节结论，例如：

- 不写：\`系统架构\`；写：\`Coordinator 不负责干活，而是负责决定谁先干什么\`。
- 不写：\`实验结果\`；写：\`71% 很亮眼，但还不能当成独立复现结果\`。
- 不写：\`局限性\`；写：\`证伪者太强，可能把真正的新想法提前杀掉\`。

## 技术保真

通俗化不是删掉技术条件。对于每个关键机制，至少保留：

1. 输入是什么；
2. 谁执行；
3. 反馈从哪里来；
4. 什么条件下停止或失败。

## 结尾

不要重复全文。用三句话收束：

1. 这项技术真正改变了什么；
2. 哪个设计最值得借鉴；
3. 目前最大的未知或风险是什么。
`,
};

const VOLC_DEVINSTANCE = `---
name: volc-devinstance
description: "火山引擎机器学习平台（veMLP）开发机管理与算力作业探针。支持快速查询开发机状态、机内正在运行的计算任务与收敛进度、安全开机、以及带作业防断保护的关机操作。当用户询问开发机状态、查看计算作业进度、关闭/开启开发机时使用。"
---

# 火山引擎开发机控制与算力作业探针 (Volcengine DevInstance & Workload Probe)

本 Skill 用于管理火山引擎机器学习平台开发机（DevInstance）生命周期，具备**双层真值感知**、**作业防断拦截**与**默认安全防护（Fail-Closed）**。

## 核心机制：双层状态判定

1. **第一层（云端服务器级别）**：通过火山 OpenAPI 快速查询开发机硬件是 \`Running\` 还是 \`Stopped\`。
2. **第二层（机内计算进程级别）**：当机器处于 \`Running\` 时，自动通过 SSH 探针进入内部，探测：
   - 是否有活跃的 \`vasp_*\`、\`python train\`、\`torch\`、\`gpumd\` 等高密集计算进程；
   - 当前正在执行的后台 tmux 任务名称与对应的工作目录；
   - 提取最新一行收敛日志（\`OSZICAR\`、\`vasp.log\`、\`train.log\`、\`nohup.out\`）；
   - 当前 CPU 负载（Load Average）。

## 依赖说明

- 需要安装火山引擎机器学习平台 CLI \`mlp\`（置于 PATH 中或 \`~/.volc/bin/mlp\`，亦可通过环境变量 \`VOLC_MLP_BIN\` 指定）。
- SSH 登录凭证优先读取 \`~/.ssh/config\`，亦可通过环境变量 \`VOLC_SSH_KEY\` 自定义。

## 常用命令

所有操作通过底层确定性脚本执行：\`~/.feishu-agent/skills/volc-devinstance/devctl <action> [target]\`

1. **查看所有个人开发机列表**：
   \`\`\`bash
   ~/.feishu-agent/skills/volc-devinstance/devctl list
   \`\`\`

2. **查询开发机详情与机内计算进度**（最常用）：
   \`\`\`bash
   ~/.feishu-agent/skills/volc-devinstance/devctl status <instance-name-or-id>
   \`\`\`
   - 若机器空闲，提示可以安全关机以节省算力费用；
   - 若有算例在跑，输出算例名称、目录、收敛步日志并发出不要关机的警告。

3. **启动开发机**：
   \`\`\`bash
   ~/.feishu-agent/skills/volc-devinstance/devctl start <instance-name-or-id>
   \`\`\`

4. **安全关机（带防误杀拦截保护）**：
   \`\`\`bash
   ~/.feishu-agent/skills/volc-devinstance/devctl stop <instance-name-or-id>
   \`\`\`
   - **防误杀机制**：
     1. 若检测到机内有正在运行的计算作业（训练/模拟/高负载），**直接拦截关机**；
     2. 若网络抖动导致 SSH 探针无法连接，默认安全（Fail-Closed）**强行拦截关机**，防止盲目杀掉任务；
     3. 若用户确定要强制关机，必须显式附加 \`--force\`：\`devctl stop <target> --force\`。

## 对抗安全规则（Agent 交互规范）

- **多重重名拦截**：若输入的简写匹配到多台机器，工具会拒绝执行并输出候选列表，Agent 必须引导用户明确指定全名或 ID。
- **凭证安全**：绝不在对话和日志中回显任何 AK/SK 或 Token。
- **关机确认**：当用户在飞书请求关机时，Agent 必须先执行 \`status\` 汇报当前是否有任务在跑，并在确认安全后再关机。
`;

const VOLC_DEVCTL = `#!/usr/bin/env python3
"""
devctl - Volcengine ML Platform DevInstance CLI Helper
Hardened with:
 1. Fail-closed shutdown protection (no kill if probe unreachable unless --force)
 2. Multi-match collision disambiguation
 3. Generalized task/load detection (VASP, Python/PyTorch, GPUMD)
 4. Bounded timeouts & zero-hanging
 5. Portable path resolution (env vars, PATH, ~/.volc, ~/.ssh)
"""
import sys
import json
import subprocess
import os
import shutil
import time

def find_mlp_bin():
    """Resolve the mlp CLI binary portably:
    1. VOLC_MLP_BIN environment variable
    2. shutil.which("mlp") in PATH
    3. Common user install path: ~/.volc/bin/mlp
    4. System-wide / user-local paths: ~/bin/mlp, ~/.local/bin/mlp, /usr/local/bin/mlp
    """
    env_bin = os.environ.get("VOLC_MLP_BIN")
    if env_bin and os.path.isfile(env_bin) and os.access(env_bin, os.X_OK):
        return env_bin
    which_bin = shutil.which("mlp")
    if which_bin:
        return which_bin
    candidates = [
        os.path.expanduser("~/.volc/bin/mlp"),
        os.path.expanduser("~/bin/mlp"),
        os.path.expanduser("~/.local/bin/mlp"),
        "/usr/local/bin/mlp",
    ]
    for c in candidates:
        if os.path.isfile(c) and os.access(c, os.X_OK):
            return c
    return None

def resolve_ssh_key(name):
    """Resolve identity file from ~/.ssh/config or conventional paths.
    Prioritizes VOLC_SSH_KEY, ~/.ssh/config Host entry, then conventional candidate keys.
    """
    env_key = os.environ.get("VOLC_SSH_KEY")
    if env_key:
        expanded = os.path.expanduser(env_key)
        if os.path.exists(expanded):
            return expanded

    config_path = os.path.expanduser("~/.ssh/config")
    if os.path.exists(config_path):
        current_host = None
        try:
            with open(config_path, "r", errors="ignore") as f:
                for line in f:
                    line = line.strip()
                    if line.lower().startswith("host "):
                        parts = line.split()
                        if len(parts) > 1:
                            current_host = parts[1].strip().lower()
                    elif current_host and current_host == name.lower() and line.lower().startswith("identityfile"):
                        parts = line.split()
                        if len(parts) > 1:
                            key = os.path.expanduser(parts[1].strip())
                            if os.path.exists(key):
                                return key
        except Exception:
            pass

    candidates = [
        f"~/.ssh/keys/{name}",
        f"~/.ssh/keys/{name.lower()}",
        f"~/.ssh/{name}",
        f"~/.ssh/{name.lower()}",
        "~/.ssh/vasp",
        "~/.ssh/id_ed25519",
        "~/.ssh/id_rsa",
    ]
    for c in candidates:
        p = os.path.expanduser(c)
        if os.path.exists(p):
            return p
    return os.path.expanduser("~/.ssh/id_rsa")

def run_mlp(*args):
    mlp_bin = find_mlp_bin()
    if not mlp_bin:
        return None, (
            "未找到火山引擎 mlp CLI。请先安装 mlp（可置于 ~/.volc/bin/mlp 或加入 PATH），"
            "或设置环境变量 VOLC_MLP_BIN 指向可执行文件。"
        )
    cmd = [mlp_bin, "devinstance"] + list(args)
    res = subprocess.run(cmd, capture_output=True, text=True)
    if res.returncode != 0:
        return None, res.stderr.strip() or res.stdout.strip()
    return res.stdout.strip(), None

def get_my_instances():
    out, err = run_mlp("list", "--mine", "--output", "json")
    if err:
        return [], err
    if not out:
        return [], None
    try:
        return json.loads(out), None
    except Exception as e:
        return [], str(e)

def resolve_instance(identifier):
    items, err = get_my_instances()
    if err:
        return None, [], err
    # 1. Exact ID match
    for it in items:
        if it.get("Id") == identifier:
            return it, [], None
    # 2. Exact Name match
    exact_names = [it for it in items if it.get("Name") == identifier]
    if len(exact_names) == 1:
        return exact_names[0], [], None
    elif len(exact_names) > 1:
        return None, exact_names, None
    
    # 3. Fuzzy Name match (case-insensitive)
    fuzzy = [it for it in items if identifier.lower() in it.get("Name", "").lower()]
    if len(fuzzy) == 1:
        return fuzzy[0], [], None
    elif len(fuzzy) > 1:
        return None, fuzzy, None
        
    return None, [], None

def probe_instance_workload(ip, port, key_path=None):
    """Probe inside running instance via SSH with fail-closed state tracking."""
    ssh_user = os.environ.get("VOLC_SSH_USER", "root")
    remote_script = r'''
python3 - << 'INNER'
import subprocess, os, json

# 1. Tmux sessions
sessions = []
try:
    res = subprocess.run(["tmux", "ls"], capture_output=True, text=True)
    if res.returncode == 0:
        for line in res.stdout.strip().splitlines():
            if line:
                sessions.append(line.split(":")[0].strip())
except Exception:
    pass

# 2. VASP process count
try:
    res = subprocess.run(["pgrep", "-c", "vasp_"], capture_output=True, text=True)
    vasp_ranks = int(res.stdout.strip())
except Exception:
    vasp_ranks = 0

# 3. General compute processes (python/train/gpumd)
general_jobs = []
try:
    res = subprocess.run(["pgrep", "-fa", "gpumd|python.*train|torch"], capture_output=True, text=True)
    general_jobs = [l.strip() for l in res.stdout.strip().splitlines() if l.strip()]
except Exception:
    pass

# 4. Detailed task progress
tasks = []
for s in sessions:
    res = subprocess.run(["tmux", "list-panes", "-t", s, "-F", "#{pane_current_path}"], capture_output=True, text=True)
    cwd = res.stdout.strip().splitlines()[0] if res.stdout.strip() else "未知目录"
    
    recent_info = ""
    for log_name in ("OSZICAR", "vasp.log", "train.log", "nohup.out"):
        target_log = os.path.join(cwd, log_name)
        if os.path.isfile(target_log):
            try:
                with open(target_log, "r", errors="ignore") as f:
                    lines = f.readlines()
                    if lines:
                        recent_info = f"{log_name}: {lines[-1].strip()}"
                        break
            except: pass
    
    tasks.append({"session": s, "directory": cwd, "recent_log": recent_info})

load1, load5, load15 = os.getloadavg()

print(json.dumps({
    "success": True,
    "vasp_ranks": vasp_ranks,
    "general_jobs_count": len(general_jobs),
    "load_1m": load1,
    "load_5m": load5,
    "tasks": tasks
}))
INNER
'''
    cmd = [
        "ssh", "-o", "StrictHostKeyChecking=no", "-o", "UserKnownHostsFile=/dev/null",
        "-o", "ConnectTimeout=4", "-p", str(port),
    ]
    if key_path and os.path.exists(key_path):
        cmd.extend(["-i", key_path])
    cmd.extend([f"{ssh_user}@{ip}", remote_script])
    res = subprocess.run(cmd, capture_output=True, text=True)
    if res.returncode != 0:
        return {"success": False, "error": res.stderr.strip() or "SSH connection failed"}
    try:
        for line in res.stdout.strip().splitlines():
            line = line.strip()
            if line.startswith("{") and line.endswith("}"):
                d = json.loads(line)
                d["success"] = True
                return d
        return {"success": False, "error": "Invalid probe payload"}
    except Exception as e:
        return {"success": False, "error": str(e)}

def cmd_list():
    items, err = get_my_instances()
    if err:
        print(f"❌ 获取开发机列表失败: {err}")
        return
    if not items:
        print("未找到当前账号名下的开发机。")
        return
    print(f"{'名称 (NAME)':20s} | {'状态 (STATE)':10s} | {'实例ID (ID)':26s} | {'规格 (FLAVOR)':20s} | {'SSH端口'}")
    print("-" * 95)
    for it in items:
        name = it.get("Name", "-")
        st = it.get("Status", {}).get("State", "-")
        iid = it.get("Id", "-")
        flav = it.get("ResourceClaim", {}).get("InstanceTypeId", "-")
        ports = it.get("Ports", [])
        ssh_info = "-"
        for p in ports:
            if p.get("Name") == "SSH连接" and p.get("Status", {}).get("State") == "Available":
                ip = p.get("ExternalIp")
                port = p.get("ExternalPort")
                if ip and port:
                    ssh_info = f"{ip}:{port}"
        print(f"{name:20s} | {st:10s} | {iid:26s} | {flav:20s} | {ssh_info}")

def cmd_status(target):
    it, matches, err = resolve_instance(target)
    if err:
        print(f"❌ 错误: {err}")
        return
    if matches:
        print(f"❌ 错误：输入 '{target}' 匹配到多个开发机，请使用完整名称或 ID 明确指定：")
        for m in matches:
            print(f"  - {m.get('Name')} ({m.get('Id')}) [{m.get('Status',{}).get('State')}]")
        return
    if not it:
        print(f"❌ 未找到与 '{target}' 匹配的开发机。请执行 'devctl list' 查看列表。")
        return
    
    iid = it.get("Id")
    out, err = run_mlp("get", "--id", iid, "--output", "json")
    if not out:
        print(f"获取实例详情失败: {err}")
        return
    
    d = json.loads(out)
    st = d.get("Status", {}).get("State")
    msg = d.get("Status", {}).get("Message", "")
    name = d.get("Name")
    flav = d.get("ResourceClaim", {}).get("InstanceTypeId")
    vol = d.get("Volume", {}).get("Size")
    ports = d.get("Ports", [])
    launch_time = d.get("LaunchTime", "-")
    
    print(f"=== 云端开发机基础状态: {name} ===")
    print(f"实例 ID:     {iid}")
    print(f"开机状态:    {st} {('(' + msg + ')') if msg else ''}")
    print(f"计算规格:    {flav}")
    print(f"系统盘:      {vol} GiB")
    print(f"启动时间:    {launch_time}")
    
    key_path = resolve_ssh_key(name)
    ssh_ip, ssh_port = None, None
    ssh_user = os.environ.get("VOLC_SSH_USER", "root")
    for p in ports:
        if p.get("Name") == "SSH连接":
            p_st = p.get("Status", {}).get("State")
            ip = p.get("ExternalIp")
            port = p.get("ExternalPort")
            if p_st == "Available" and ip and port:
                ssh_ip, ssh_port = ip, port
                key_arg = f"-i {key_path} " if os.path.exists(key_path) else ""
                print(f"SSH 直连:    ssh {key_arg}-p {port} {ssh_user}@{ip}  (或直接: ssh {name.lower()})")
        elif p.get("Name") == "WebIDE" and st == "Running":
            url = p.get("APIGPublicUrl")
            if url:
                print(f"WebIDE:      {url}")
    
    print("\\n--- 机器内部任务与负载深度探针 ---")
    if st != "Running":
        print(f"机器处于 {st} 状态，未在运行任何计算任务。")
        return
    
    if not (ssh_ip and ssh_port):
        print("未检测到可用的公网 SSH 端口，无法执行内部探针。")
        return
    
    probe = probe_instance_workload(ssh_ip, ssh_port, key_path=key_path)
    if not probe or not probe.get("success"):
        err_msg = probe.get("error") if probe else "连接超时"
        print(f"⚠️ SSH 探针无法连接 ({err_msg})，请稍后重试。")
        return
    
    ranks = probe.get("vasp_ranks", 0)
    gjobs = probe.get("general_jobs_count", 0)
    load = probe.get("load_1m", 0)
    tasks = probe.get("tasks", [])
    
    if ranks > 0 or gjobs > 0 or load > 2.0:
        print(f"⚡ 【正在计算中】")
        if ranks > 0:
            print(f"  • VASP 并行核心数:  {ranks} ranks (高负荷)")
        if gjobs > 0:
            print(f"  • 其他活跃计算进程:  {gjobs} 个 (Python/PyTorch/GPUMD)")
        print(f"  • CPU 1分钟平均负载: {load:.2f}")
        print(f"  • 活跃后台任务会话:  {len(tasks)} 个")
        for t in tasks:
            print(f"    ▶ 会话名:   {t.get('session')}")
            print(f"      计算目录: {t.get('directory')}")
            if t.get("recent_log"):
                print(f"      最新进度: {t.get('recent_log')}")
        print("⚠️ 警告：当前机器处于繁忙计算状态，切勿随意关机！")
    else:
        print(f"💤 【当前空闲】机内无活跃计算进程 (CPU 负载: {load:.2f})。")
        if tasks:
            print(f"存在 {len(tasks)} 个已结束或挂起的 tmux 会话：")
            for t in tasks:
                print(f"  - {t.get('session')} ({t.get('directory')})")
        print("✅ 提示：如果不需要继续计算，建议关机以节省算力费用。")

def cmd_start(target):
    it, matches, err = resolve_instance(target)
    if err:
        print(f"❌ 错误: {err}")
        return
    if matches:
        print(f"❌ 错误：输入 '{target}' 匹配到多个开发机，请使用全名指定：")
        for m in matches:
            print(f"  - {m.get('Name')} ({m.get('Id')})")
        return
    if not it:
        print(f"❌ 未找到与 '{target}' 匹配的开发机。")
        return
    iid = it.get("Id")
    name = it.get("Name")
    print(f"正在启动开发机 {name} ({iid})...")
    out, err = run_mlp("start", "--id", iid)
    if err:
        print(f"启动命令发送失败: {err}")
        return
    print("启动指令已下发，正在轮询状态...")
    for _ in range(8):
        time.sleep(3)
        chk, _ = run_mlp("get", "--id", iid, "--output", "json")
        if chk:
            st = json.loads(chk).get("Status", {}).get("State")
            if st in ("Running", "Deploying"):
                print(f"当前状态: {st}")
                if st == "Running":
                    cmd_status(iid)
                return
    print("机器已进入启动队列，请稍后查询状态。")

def cmd_stop(target, force=False):
    it, matches, err = resolve_instance(target)
    if err:
        print(f"❌ 错误: {err}")
        return
    if matches:
        print(f"❌ 错误：输入 '{target}' 匹配到多个开发机，请使用全名指定：")
        for m in matches:
            print(f"  - {m.get('Name')} ({m.get('Id')})")
        return
    if not it:
        print(f"❌ 未找到与 '{target}' 匹配的开发机。")
        return
    iid = it.get("Id")
    name = it.get("Name")
    
    out, _ = run_mlp("get", "--id", iid, "--output", "json")
    if out:
        d = json.loads(out)
        st = d.get("Status", {}).get("State")
        if st == "Running" and not force:
            ssh_ip, ssh_port = None, None
            for p in d.get("Ports", []):
                if p.get("Name") == "SSH连接" and p.get("Status", {}).get("State") == "Available":
                    ssh_ip = p.get("ExternalIp")
                    ssh_port = p.get("ExternalPort")
                    break
            
            if ssh_ip and ssh_port:
                key_path = resolve_ssh_key(name)
                probe = probe_instance_workload(ssh_ip, ssh_port, key_path=key_path)
                # 1. Fail-closed: probe failed
                if not probe or not probe.get("success"):
                    print(f"🛑 安全拦截：无法通过 SSH 确认机内计算状态 (网络未连通或探测超时)。")
                    print(f"为防止强行关机导致正在跑的作业被杀死，已阻止关机！")
                    print(f"若确定机器安全且执意关机，请附加参数：devctl stop {name} --force")
                    return
                # 2. Fail-closed: active calculation running
                ranks = probe.get("vasp_ranks", 0)
                gjobs = probe.get("general_jobs_count", 0)
                load = probe.get("load_1m", 0)
                if ranks > 0 or gjobs > 0 or load > 2.0:
                    tasks = [t.get("session") for t in probe.get("tasks", [])]
                    print(f"🚨 严重警告：开发机 {name} 内部正在进行密集计算！")
                    if ranks > 0: print(f"  • VASP 进程占用: {ranks} 核")
                    if gjobs > 0: print(f"  • 其他计算进程: {gjobs} 个")
                    print(f"  • 系统负载: {load:.2f}")
                    print(f"  • 运行中的会话: {tasks}")
                    print(f"此时关机会导致计算直接中断丢步！")
                    print(f"若确实需要强制关机，请附加参数：devctl stop {name} --force")
                    return

    print(f"正在停止开发机 {name} ({iid}) 以释放算力...")
    out, err = run_mlp("stop", "--id", iid)
    if err:
        print(f"停止命令发送失败: {err}")
        return
    print("停止指令已下发，正在等待关机确认...")
    for _ in range(8):
        time.sleep(3)
        chk, _ = run_mlp("get", "--id", iid, "--output", "json")
        if chk:
            st = json.loads(chk).get("Status", {}).get("State")
            if st == "Stopped":
                print(f"开发机 {name} 已成功关机 (Stopped)。算力已释放。")
                return
    print(f"开发机 {name} 正在关机中，请稍后确认。")

def main():
    if len(sys.argv) < 2:
        print("用法: devctl <list | status <name/id> | start <name/id> | stop <name/id> [--force]>")
        sys.exit(1)
    
    action = sys.argv[1].lower()
    if action == "list":
        cmd_list()
    elif action in ("status", "get", "info"):
        if len(sys.argv) < 3:
            print("错误: 请指定开发机名称或 ID，例如: devctl status my-instance")
            sys.exit(1)
        cmd_status(sys.argv[2])
    elif action in ("start", "on", "boot"):
        if len(sys.argv) < 3:
            print("错误: 请指定开发机名称或 ID，例如: devctl start my-instance")
            sys.exit(1)
        cmd_start(sys.argv[2])
    elif action in ("stop", "off", "shutdown"):
        if len(sys.argv) < 3:
            print("错误: 请指定开发机名称或 ID，例如: devctl stop my-instance")
            sys.exit(1)
        cmd_stop(sys.argv[2], force="--force" in sys.argv)
    else:
        print(f"未知操作 '{action}'。支持的操作: list, status, start, stop")
        sys.exit(1)

if __name__ == "__main__":
    main()
`;

const VOLC_DEVINSTANCE_FILES: Record<string, string> = {
  devctl: VOLC_DEVCTL,
};

export const DEFAULT_SKILLS: DefaultSkill[] = [
  { name: "feishu-skill-maker", body: FEISHU_SKILL_MAKER },
  { name: "feishu-find-skill", body: FEISHU_FIND_SKILL },
  { name: "feishu-latex-rendering", body: FEISHU_LATEX_RENDERING },
  { name: "process-optimization-biweekly", body: PROCESS_OPTIMIZATION_BIWEEKLY },
  { name: "deslop-zh", body: DESLOP_ZH },
  { name: "feishu-pro-diagram", body: FEISHU_PRO_DIAGRAM },
  { name: "feishu-package-curator", body: FEISHU_PACKAGE_CURATOR },
  { name: "feishu-tech-note-writer", body: FEISHU_TECH_NOTE_WRITER, files: FEISHU_TECH_NOTE_WRITER_FILES },
  { name: "volc-devinstance", body: VOLC_DEVINSTANCE, files: VOLC_DEVINSTANCE_FILES },
];
